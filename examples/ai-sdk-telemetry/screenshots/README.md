# Screenshots

Drop the images referenced by `tutorial.md` here. Generate data first
(`npm run setup && npm run seed`), then `npm run dev` and open
<http://localhost:3000>.

| File | What to capture |
| --- | --- |
| `dashboard-full.png` | The entire dashboard page (hero shot). |
| `stat-cards.png` | The top row of four stat cards only. |
| `latency-chart.png` | The "Tool latency" card (p50/p95/p99 grouped bars). |
| `tokens-and-reasons.png` | The "Average tokens per function" bar + "Finish reasons" donut. |
| `recent-table.png` | The "Recent generations" table. |
