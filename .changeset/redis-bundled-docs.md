---
"@upstash/redis": patch
---

Include TypeScript source and Redis SDK documentation in the npm package for offline reference and coding agents. Command JSDoc links each method to its bundled docs page and notes automatic JSON deserialization on commands that return stored values.
